@model IEnumerable<SOFT331.Models.Ticket>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Discount.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fare.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Timetable.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wheelchair)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Discount.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fare.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Timetable.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wheelchair)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
